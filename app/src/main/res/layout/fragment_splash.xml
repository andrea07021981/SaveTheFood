<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:bind="http://schemas.android.com/tools">

    <data>
        <variable
            name="splashViewModel"
            type="com.example.savethefood.splash.SplashViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".splash.SplashFragment"
        android:background="@color/colorPrimary">

        <ImageView
            android:id="@+id/chef_imageview"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:contentDescription="chef"
            android:transitionName="chef_imageview"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            bind:loadViewAnimation="@{splashViewModel.animationResource}"
            app:srcCompat="@drawable/ic_food" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
